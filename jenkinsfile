pipeline {
    agent {
        label "worker"
    }

    stages {
        stage('checkout'){
            steps {
                git 'https://github.com/mayurpandit25/student-management.git'
            }
        }
    }
}