apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  labels:
    app: mysql
  namespace: student
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi